/**
 * DEPRECATED HERO COMPONENT
 * This file is deprecated and should not be used.
 * Use /components/Hero.tsx instead.
 * 
 * This file exists only to prevent import errors.
 * The main application uses the root-level components.
 */

// Re-export the main Hero component to prevent errors
export { Hero } from '../../components/Hero';

// Default export for compatibility
import { Hero } from '../../components/Hero';
export default Hero;