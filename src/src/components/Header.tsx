/**
 * DEPRECATED HEADER COMPONENT
 * This file is deprecated and should not be used.
 * Use /components/Header.tsx instead.
 * 
 * This file exists only to prevent import errors.
 * The main application uses the root-level components.
 */

// Re-export the main Header component to prevent errors
export { Header } from '../../components/Header';

// Default export for compatibility
import { Header } from '../../components/Header';
export default Header;