# DEPRECATED SRC DIRECTORY

This directory contains outdated files that conflict with the main application.
The main application now uses the root-level structure:

- `/App.tsx` (main app)
- `/index.tsx` (entry point) 
- `/components/` (all components)

These src files are no longer used and should be ignored.