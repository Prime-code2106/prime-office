[build]
  command = "npm run build"
  publish = "dist"

# Single Page Applications require a rewrite rule to redirect all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
